
{
  "entities": {
    "SongRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SongRequest",
      "type": "object",
      "description": "Represents a song request submitted by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the song request."
        },
        "studentId": {
          "type": "string",
          "description": "Reference to the Student who submitted the request."
        },
        "studentName": {
          "type": "string",
          "description": "Denormalized name of the student for easy display."
        },
        "title": {
          "type": "string",
          "description": "Title of the requested song."
        },
        "karaokeUrl": {
          "type": "string",
          "description": "URL to the karaoke video for the song.",
          "format": "uri"
        },
        "submissionDate": {
          "type": "string",
          "description": "Date and time when the song request was submitted (server timestamp).",
          "format": "date-time"
        },
        "order": {
            "type": "number",
            "description": "The order of the song in the queue."
        }
      },
      "required": [
        "id",
        "studentId",
        "studentName",
        "title",
        "karaokeUrl",
        "submissionDate",
        "order"
      ]
    },
    "Student": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Student",
      "type": "object",
      "description": "Represents a student or admin who can submit song requests.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (matches Firebase Auth UID)."
        },
        "name": {
          "type": "string",
          "description": "Name of the user."
        },
        "role": {
          "type": "string",
          "description": "The role of the user.",
          "enum": ["student", "admin"]
        }
      },
      "required": [
        "id",
        "name",
        "role"
      ]
    },
    "AuditLog": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "AuditLog",
        "type": "object",
        "description": "Represents a log of an action taken in the system.",
        "properties": {
            "id": { "type": "string" },
            "timestamp": { "type": "string", "format": "date-time" },
            "actorId": { "type": "string" },
            "actorName": { "type": "string" },
            "action": { "type": "string" },
            "details": { "type": "string" }
        },
        "required": ["id", "timestamp", "actorId", "actorName", "action", "details"]
    }
  },
  "auth": {
    "providers": [
      "email-password"
    ]
  },
  "firestore": {
    "/students/{studentId}": {
      "schema": {
        "$ref": "#/entities/Student"
      },
      "description": "Stores student and admin profiles. Each document represents a user."
    },
    "/song_requests/{songRequestId}": {
        "schema": {
            "$ref": "#/entities/SongRequest"
        },
        "description": "Stores all song requests in a single collection for easier querying and sorting by admins/owners."
    },
    "/audit_logs/{logId}": {
        "schema": {
            "$ref": "#/entities/AuditLog"
        },
        "description": "Stores a chronological log of all significant actions taken in the application."
    }
  }
}
