
{
  "entities": {
    "SongRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SongRequest",
      "type": "object",
      "description": "Represents a song request submitted by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the song request."
        },
        "studentId": {
          "type": "string",
          "description": "Legacy field, can be 'anonymous' or a user ID."
        },
        "requesterName": {
          "type": "string",
          "description": "Denormalized name of the person who made the request for easy display."
        },
        "title": {
          "type": "string",
          "description": "Title of the requested song."
        },
        "karaokeUrl": {
          "type": "string",
          "description": "URL to the karaoke video for the song.",
          "format": "uri"
        },
        "submissionDate": {
          "type": "string",
          "description": "Date and time when the song request was submitted (server timestamp).",
          "format": "date-time"
        },
        "order": {
            "type": "number",
            "description": "The order of the song in the queue."
        }
      },
      "required": [
        "id",
        "requesterName",
        "title",
        "karaokeUrl",
        "submissionDate",
        "order"
      ]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/song_requests/{songRequestId}": {
        "schema": {
            "$ref": "#/entities/SongRequest"
        },
        "description": "Stores all song requests in a single collection for easier querying and sorting by admins."
    }
  }
}
