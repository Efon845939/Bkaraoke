
'use client';

import { Logo } from '@/components/logo';

export function PageHeader() {
  return (
    // Link elementi dış katmandan (admin-dashboard) geldiği için buradan kaldırıldı.
    <Logo />
  );
}
